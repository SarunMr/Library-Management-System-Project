from tkinter import *
from PIL import Image, ImageTk

def create_manage_books_frame(root):
    manage_books_frame = Frame(root, bg="#f0f0f0")
    manage_books_frame.place(x=0, y=0, width=900, height=700)

    # ... (rest of the manage books frame code)

    return manage_books_frame
