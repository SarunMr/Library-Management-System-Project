from tkinter import *
from PIL import Image, ImageTk

def create_view_users_frame(root):
    view_users_frame = Frame(root, bg="#f0f0f0")
    view_users_frame.place(x=0, y=0, width=900, height=700)

    # ... (rest of the view users frame code)

    return view_users_frame
