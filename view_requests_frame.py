from tkinter import *
from PIL import Image, ImageTk

def create_view_requests_frame(root):
    view_requests_frame = Frame(root, bg="#f0f0f0")
    view_requests_frame.place(x=0, y=0, width=900, height=700)

    # ... (rest of the view requests frame code)

    return view_requests_frame
