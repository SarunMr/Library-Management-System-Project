from tkinter import *
from PIL import Image, ImageTk

def create_dashboard_frame(root):
    dashboard_frame = Frame(root, bg="#f0f0f0")
    dashboard_frame.place(x=0, y=0, width=900, height=700)

    # ... (rest of the dashboard frame code)

    return dashboard_frame
